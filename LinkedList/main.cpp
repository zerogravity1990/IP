#include "linked.h"

string dizionario[] = {"alessandro", "leonida", "elon", "cesare", "nonna maria"};

int main(){

string parola = "";
cin >> parola;
//Word * w = new Word();
//insertData(w,parola);
printWord(newWord(parola));

}
